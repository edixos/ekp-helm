{{- range $config := .Values.referenceGrants -}}
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: {{ $config.name }}
  namespace: {{ $config.namespace }}
spec:
{{ toYaml $config.spec | indent 2 }}
---
{{ end -}}